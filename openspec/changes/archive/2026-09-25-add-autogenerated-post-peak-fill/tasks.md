# Tasks

## 1. Pure post-Peak fill planner

- [x] 1.1 [P] Add table-driven tests in `src/core/engine/__tests__/postPeakFill.test.ts` for automatic start at `peakDay + postPeakDays + 1`, `after-user-low` starting only after a qualifying user Low, leaving earlier gaps blank, ignoring a Low before the configured post-Peak tail boundary, open/closed-cycle boundaries, no future dates, active deletion suppressions, the 30-record cap, and window-change cap reset; run the targeted suite to establish the expected failures.
- [x] 1.2 Add strict pure-engine types for the two fill modes and inference provenance/planner inputs, keeping them free of React, Dexie, and browser APIs; run the TypeScript check and verify the new types compile.
- [x] 1.3 Implement the pure post-Peak fill planner in `src/core/engine/postPeakFill.ts` using the existing Peak/window semantics and date utilities; run the new planner tests plus `src/core/engine/__tests__/marquette.test.ts` and verify all pass.

## 2. Provenance and store reconciliation

- [x] 2.1 [P] Add store tests in `src/core/store/__tests__/postPeakFill.store.test.ts` for legacy records defaulting to user provenance, automatic generation, user-anchor mode, 30-record cap, future-date blocking, generated/user edit precedence, persistent deletion suppression, suppression clearing, backfilled-Menses invalidation, new-Peak invalidation, settings-mode changes, `postPeakDays` changes, and algorithm-off preservation; run the targeted suite to establish the expected failures.
- [x] 2.2 Extend `src/core/store/entities.ts` and `src/core/store/repositories.ts` with optional inferred-data provenance and inference lineage, treating absent legacy provenance as user-authored; add the persisted `postPeakFillMode` and post-Peak suppression settings with safe defaults and verify legacy hydration/round-trip tests.
- [x] 2.3 Implement transactional reconciliation in `src/core/store/useAppStore.ts` (and a focused store helper if needed) for user writes, deletes, hydration, and relevant settings changes: derive structure from authoritative user evidence, create eligible inferred Low rows, honor persistent deletion suppressions, remove only stale inferred rows, preserve user rows, and refresh engine output; run the new store tests and existing placement tests.
- [x] 2.4 Verify generated rows are assigned stable cycle/day values and that backfilled Menses, High/Peak changes, user overrides, persistent deletion suppressions, and stale-tail deletion converge without duplicate records or future writes; run the complete `src/core/store/__tests__/` suite.

## 3. Calendar provenance and reconciled display

- [x] 3.1 [P] Add Calendar component/grid tests for an autogenerated Low marker, the existing derived status color, user editing/deletion, `after-user-low` gaps remaining blank, new-Menses tail reconciliation, and no generated future cells; run the targeted Calendar tests to establish the expected failures.
- [x] 3.2 Extend `src/features/calendar/grid.ts`, `day-cell.tsx`, `index.tsx`, and `quick-entry.tsx` to expose inferred provenance, retain the normal stored Low marker, render an accessible assumed-data marker, make explicit user saves authoritative, and keep explicitly deleted inferred dates absent; run the Calendar test suite and verify the dialog behavior.
- [x] 3.3 Update the Calendar legend/copy and any screen-reader labels needed to distinguish assumed readings from user input without presenting inferred data as future or user-entered; run the relevant Calendar tests and verify the rendered labels.

## 4. Settings control and algorithm interaction

- [x] 4.1 [P] Add Settings tests for the default `auto-after-window` value, persistence of both modes, immediate reconciliation on mode changes, algorithm-off preservation/no-new-generation, and post-Peak-day boundary changes; run the targeted Settings tests to establish the expected failures.
- [x] 4.2 Add the fill-mode selector to `src/features/settings/core-section.tsx`, persist it and post-Peak suppression state through the existing settings path, and ensure mode/algorithm/post-Peak updates invoke the same reconciliation path as day-record writes; run the Settings and store tests and verify the control survives reload.

## 5. Documentation and validation gates

- [x] 5.1 Update user-facing settings/calendar documentation to describe the two fill modes, inferred-data marker, persistent deletion suppression, 30-record cap, and the fact that generated rows are stored assumptions; verify the documentation matches the implemented settings labels and behavior.
- [x] 5.2 Run `pnpm test`, `pnpm lint`, and `pnpm build` after all implementation tasks; verify all commands pass with no new dependency and no TypeScript, test, lint, or production-build regressions.

## 6. Verification follow-ups

- [x] 6.1 Add a planner regression for a cycle whose historical calendar end is earlier than the configured post-Peak tail boundary, asserting generated dates start at `peakDay + postPeakDays + 1` while derived status retains the historical end; run the targeted engine tests.
- [x] 6.2 Add a Settings/store scenario that disables the algorithm, verifies no new generated rows are created, re-enables the algorithm, and verifies reconciliation resumes; run the targeted Settings and store tests.
- [x] 6.3 Add Calendar component coverage for a backfilled Menses re-render, verifying the stale inferred tail is removed and the new cycle boundary is shown; run the targeted Calendar tests.
- [x] 6.4 Add direct engine/store coverage asserting generated Low records are excluded from Peak evidence and cannot shift the computed fertile-window begin or end; run the targeted engine and store tests.
- [x] 6.5 Add store coverage for a non-structural BBT or notes edit within an eligible generated tail, verifying the generated rows remain eligible and the user fields are preserved; run the targeted store tests.
