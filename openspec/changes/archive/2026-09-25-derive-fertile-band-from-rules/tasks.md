# Tasks

Tests-first for anything in `src/core/engine` (config rule: `pnpm test` is required for engine changes). Every task states its own verification.

## 1. Engine: day results span the derived window

- [x] 1.1 Add failing table-driven cases to `src/core/engine/__tests__/marquette.test.ts` for dense day coverage: monitor Peak on day 12 with no records on 13–16 yields `fertile` on each; days 1–5 yield `pre-fertile`; a closed cycle yields a result for every day of its length; an open cycle with no determinable window end yields results from window begin through the current day and none beyond. Verify the new cases fail against the current record-mapped `days` array.
- [x] 1.2 Implement the span and dense day results in `computeCycle` (`src/core/engine/marquette.ts`): bound the covered span by cycle length for a closed cycle and by the current day for an open one, and derive each day's status from the computed window rather than mapping over records. Verify the 1.1 cases pass and the full engine suite is green under `pnpm test src/core/engine`.
- [x] 1.3 Update the existing day-array assertions in `src/core/engine/__tests__/marquette.test.ts` that assumed `days` listed only recorded days (including the `toEqual([14, 19, 20])` case and the `day20` status lookup) to the dense shape. Verify `pnpm test src/core/engine` passes.

## 2. Engine: remove the source axis and provenance types

- [x] 2.1 [P] Add failing assertions that engine output carries no `source` field and no per-day record identity or data-origin value, and that a computed status does not depend on any stored record existing. Verify they fail.
- [x] 2.2 In `src/core/engine/types.ts` remove `DataOrigin`, `PostPeakInference`, `PostPeakSuppression`, `PostPeakFillMode`, the `DayRecordInput.dataOrigin` field, and `DayResult.source`. In `src/core/engine/marquette.ts` delete `isUserEvidence`, the `sourceForDay` helper, and all `source` assignments. Verify `pnpm test src/core/engine` passes with the 2.1 assertions green.
- [x] 2.3 Delete `src/core/engine/postPeakFill.ts` and `src/core/engine/__tests__/postPeakFill.test.ts`. Verify no file under `src/core/engine` still references `postPeakFill`, `dataOrigin`, or `inference` (`rg -n 'postPeakFill|dataOrigin|inference' src/core/engine` returns nothing) and `pnpm test src/core/engine` passes.

## 3. Status derivation helper

- [x] 3.1 Change `src/core/cycleStatus.ts` so `dayInfo` returns a bare `DayStatus` with no `source`, and update `src/core/__tests__/cycleStatus.test.ts` accordingly. Verify `pnpm test src/core/__tests__/cycleStatus.test.ts` passes.

## 4. Store: remove reconciliation and generated rows

- [x] 4.1 Add failing store tests in `src/core/store/__tests__/store.test.ts`: saving a cycle with a monitor Peak produces no `dayRecords` on the post-Peak dates; no record is ever written with a data origin; and backfilling a Menses boundary still regroups cycles correctly with a single placement pass. Verify they fail while generation is still active.
- [x] 4.2 Add failing store tests for the local-data removal: a pre-seeded autogenerated row is gone after `hydrate()`; a pre-seeded row the user edited is retained; hydrating twice removes nothing further; and restoring a document whose rows carry the legacy origin does not reintroduce them. Verify the first, second, and fourth fail while no removal exists.
- [x] 4.3 In `src/core/store/entities.ts` drop `dataOrigin`, `inference`, `postPeakFillMode`, and `postPeakSuppressions`. In `src/core/store/selectors.ts` delete `recordsForMode`. Verify the 4.1 tests fail to compile or fail assertions, confirming the fields are gone.
- [x] 4.4 In `src/core/store/useAppStore.ts` delete `reconcileGeneratedRecordsInTransaction`, `reconcileGeneratedRecords`, `suppressionFor`, and `clearSuppressionsForDate`; route mutations through a single `reconcileCycleRows` call on user records with no generation or stale-id handling; simplify `addDayRecord` and `removeDayRecord` accordingly. Verify the 4.1 cases pass.
- [x] 4.5 Add a removal routine in `src/core/store/useAppStore.ts` that filters the legacy origin through a locally-declared row shape — without reintroducing the field on `DayRecordEntity` — and bulk-deletes the matching rows. Call it from `hydrate()` in place of the deleted reconciliation, and again at the end of `restoreBackup` before `refresh()`. Verify the 4.2 cases pass and the routine is idempotent.
- [x] 4.6 Delete `src/core/store/__tests__/postPeakFill.store.test.ts`. Verify `pnpm test src/core/store` passes and `rg -n 'postPeakFill|recordsForMode' src/core/store` returns nothing, with `dataOrigin` appearing only inside the removal routine and its tests.

## 5. Backup shape

- [x] 5.1 [P] Update `src/core/backup/__tests__/fixtures.ts` and `src/core/backup/__tests__/backup.test.ts` to build and assert day records and settings without provenance or fill preferences, keeping `formatVersion` at 1. Verify the updated fixtures still exercise round-trip identity and unsynchronized marking.
- [x] 5.2 In `src/core/backup/backup.ts` remove per-record provenance validation and the fill-mode and suppression validation, and stop normalizing the legacy origin during validation so an older document's rows reach storage for the 4.5 removal to identify. Verify `pnpm test src/core/backup` passes, an exported document contains no provenance or fill-preference key, and a restored older document is accepted.

## 6. Shared visual vocabulary and tokens

- [x] 6.1 [P] Update `src/lib/__tests__/fertility-visuals.test.ts` to assert that no source-cue map, no `predictedFill`, and no assumed-marker treatment is exported, then update `src/lib/fertility-visuals.ts` to remove `FERTILITY_SOURCE_VISUALS`, the `predictedFill` field, and the `assumed` entry, and to reduce `fertilityStatusBadge` to a single-treatment signature. Verify `pnpm test src/lib` passes.
- [x] 6.2 Remove the now-unreferenced `--fertility-source-*` and `*-predicted` design tokens from `src/index.css`. Verify `rg -n 'fertility-source|status-.*-predicted' src/index.css src` shows no remaining token definitions or usages.

## 7. Calendar

- [x] 7.1 Add failing tests to `src/features/calendar/__tests__/calendar.test.tsx` and `src/features/calendar/__tests__/grid.test.ts`: with a Peak on day 12 and no records, days 13–16 resolve to `Fertile` and post-window days resolve to `After`; a past or present date inside the forecast range does not get the forecast treatment; a future date inside it still does. Verify the new cases fail against the record-gated `resolveCell`.
- [x] 7.2 In `src/features/calendar/grid.ts` drop the record gate in `resolveCell` so `info` is derived for any date inside a cycle, and scope the forecast to `dateKey > today`. Verify the 7.1 cases pass and `pnpm test src/features/calendar/__tests__/grid.test.ts` is green.
- [x] 7.3 In `src/features/calendar/day-cell.tsx` remove the `source` cue, the `data-source` attribute, the origin-based assumed asterisk, and the predicted-fill selection, keeping the forecast treatment and the single phase fill. Verify `pnpm test src/features/calendar` passes.
- [x] 7.4 In `src/features/calendar/index.tsx` remove the assumed-data legend entry and the full-detail source legend entries. In `src/features/calendar/quick-entry.tsx` remove the assumed-data explanatory paragraph. Verify the Calendar legend assertions in `calendar.test.tsx` match the reduced vocabulary.
- [x] 7.5 Delete `src/features/calendar/__tests__/postPeakFill.test.tsx` and update the remaining Calendar tests for the removed attributes and legend entries. Verify `pnpm test src/features/calendar` passes.

## 8. Status view

- [x] 8.1 [P] Update `src/features/status/__tests__/status.test.tsx` and `src/features/status/__tests__/lib.test.ts` to assert no source badge is rendered, that an unlogged in-window date still reports `fertile`, and that the next-period estimate keeps its predictive treatment. Verify the new assertions fail before the change.
- [x] 8.2 In `src/features/status/status-card.tsx` remove the source badge and the `source` prop; in `src/features/status/index.tsx` stop passing it and drop the source import path; update `src/features/status/lib.ts` if it surfaces source. Verify `pnpm test src/features/status` passes.

## 9. Cycle chart

- [x] 9.1 Add failing tests to `src/features/cycle-chart/__tests__/lib.test.tsx` and `src/features/cycle-chart/__tests__/lib.test.ts`: unrecorded days inside the fertile window carry a status, the window model exposes no `source`, and a day with no record still shows the empty monitor track. Verify they fail against the record-gated model.
- [x] 9.2 In `src/features/cycle-chart/lib.ts` resolve each day's status from the cycle result's dense day array rather than by lookup, and drop `source` from `StripDay` and `StripWindow`. In `src/features/cycle-chart/strip-chart.tsx` remove the `strokeDasharray` source selection, the `data-source` attribute, and the confirmed/predicted window label. Verify the 9.1 cases pass and `pnpm test src/features/cycle-chart` is green.

## 10. Settings

- [x] 10.1 [P] Update `src/features/settings/__tests__/settings.test.tsx` and `src/features/settings/__tests__/algorithm-off.test.tsx` to assert the fill-mode selector is absent and that algorithm-off leaves all stored records visible with no derived styling. Verify the new assertions fail before the change.
- [x] 10.2 Remove the post-Peak fill mode `Select` and its label from `src/features/settings/core-section.tsx` and drop the `PostPeakFillMode` import. Delete `src/features/settings/__tests__/postPeakFill.test.tsx` and update the remaining Settings tests. Verify `pnpm test src/features/settings` passes.

## 11. Gates

- [x] 11.1 Run `pnpm test`, `pnpm lint`, and `pnpm build` and verify all three pass with no new dependency and no change to the fertile-window begin/end rules or cycle placement.
- [x] 11.2 Verify `rg -n 'postPeakFill|postPeakSuppressions|recordsForMode|FERTILITY_SOURCE_VISUALS|predictedFill' src` returns nothing and that `dataOrigin` survives only inside the 4.5 removal routine and its tests, then confirm the `post-peak-fill` capability's nine requirements plus the `inferred` scenarios in the `marquette-engine` and `calendar` specs are the only behavior intentionally dropped.
- [x] 11.3 Manually confirm against a database that still holds pre-change rows: the previously autogenerated Low readings are gone after load, no cell shows an asterisk or a source cue, a cycle with a Peak on day 12 shows a continuous band through day 16 and an `After` band after it with no gaps, and an open cycle with no Peak shows `Fertile` from day 6 through today with forecast styling beyond.
